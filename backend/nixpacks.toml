[variables]
PYTHON_VERSION = "3.11"
POETRY_VIRTUALENVS_CREATE = "false"
PIP_NO_CACHE_DIR = "1"

[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["poetry install --no-interaction --no-ansi"]

[start]
cmd = "poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
