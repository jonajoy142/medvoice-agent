{
  "framework": "vite",
  "installCommand": "if [ -d frontend ]; then cd frontend; fi; npm ci",
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm run build && rm -rf ../dist && cp -R dist ../dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
